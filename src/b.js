export const b = (prefix) => {
  return `${prefix} b function`;
};

export const c = (prefix) => {
  return `${prefix} c function`;
};

export const d = (prefix) => {
  return `${prefix} d function`;
};
